# BGP Basic manifests

`evidence_basic.json` defines one reusable read-only capture per device, stage, and source.

`state.json` and `session_health.json` analyze the same saved evidence. They do not perform a second collection.

Route-level, policy, and service validation are not part of the public v1 capture.
